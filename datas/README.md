# data

游戏数值配置表目录（策划数据）。

- 存放物品、技能、关卡、掉落、怪物等数值表，程序只读加载，支持热更。
- 通常由策划/工具导出（Lua 表 / JSON / CSV），运行时通过只读共享数据加载。
- 与 `etc/` 的区分：`etc/` 是程序运行配置（节点、服务），本目录是游戏数据配置。
- 当前格式：Unity 导出的 JSON（`*.json`，如 `Item.json`），由 `configd` 服务解析后加载进 sharedata；`.json.meta` 为 Unity 元数据，加载时跳过。
